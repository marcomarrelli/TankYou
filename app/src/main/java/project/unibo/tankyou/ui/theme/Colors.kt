package project.unibo.tankyou.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Object defining the color palette for the light theme of the application.
 * Each color is represented as a `Color` object.
 */
object LightColors {
    /** The color for titles. */
    val Title = Color(0xFF131101)

    /** The color for regular text. */
    val Text = Color(0xFF1D1D1D)

    /** The background color for most screens. */
    val Background = Color(0xFFDAE7CA)

    /** The color for borders and dividers. */
    val Border = Color(0xFF8D9C7D)

    /** The color for disabled text. */
    val DisabledText = Color(0xFF7A7A7A)

    /** The background color for disabled elements. */
    val DisabledBackground = Color(0xFFE8F0DC)

    /** The border color for disabled elements. */
    val DisabledBorder = Color(0xFFD0DDBE)

    /** The primary color, used for main actions and highlights. */
    val Primary = Color(0xFFB6C892)

    /** The secondary color, used for complementary actions and elements. */
    val Secondary = Color(0xFFA06522)

    /** The accent color, used for drawing attention to specific elements. */
    val Accent = Color(0xFFDA8A44)
    
    /** The color for success or positive feedback (used also for cluster fullness). */
    val OK = Color(0xFF23BA39)

    /** The color for warnings or cautionary messages (used also for cluster fullness). */
    val Warning = Color(0xFFEAAD16)

    /** The color for alerts or error messages (used also for cluster fullness). */
    val Alert = Color(0xFFEC2E23)

    /** A standard white color. */
    val White = Color(0xFFFEFAE1)

    /** A standard black color. */
    val Black = Color(0xFF1E1A01)
}

/**
 * Object defining the color palette for the dark theme of the application.
 * Each color is represented as a `Color` object.
 */
object DarkColors {
    /** The color for titles. */
    val Title = Color(0xFFF8F3EC)

    /** The color for regular text. */
    val Text = Color(0xFFFEFAE1)

    /** The background color for most screens. */
    val Background = Color(0xFF283518)

    /** The color for borders and dividers. */
    val Border = Color(0xFF3A521F)

    /** The color for disabled text. */
    val DisabledText = Color(0xFF8A8A8A)

    /** The background color for disabled elements. */
    val DisabledBackground = Color(0xFF333F22)

    /** The border color for disabled elements. */
    val DisabledBorder = Color(0xFF242F18)

    /** The primary color, used for main actions and highlights. */
    val Primary = Color(0xFF5B6D37)

    /** The secondary color, used for complementary actions and elements. */
    val Secondary = Color(0xFFDDA25F)

    /** The accent color, used for drawing attention to specific elements. */
    val Accent = Color(0xFFBB6B25)

    /** The color for success or positive feedback (used also for cluster fullness). */
    val OK = Color(0xFF1A8A2B)

    /** The color for warnings or cautionary messages (used also for cluster fullness). */
    val Warning = Color(0xFFB8850F)

    /** The color for alerts or error messages (used also for cluster fullness). */
    val Alert = Color(0xFFB8211A)

    /** A standard white color. */
    val White = Color(0xFFFEFAE1)

    /** A standard black color. */
    val Black = Color(0xFF1E1A01)
}
